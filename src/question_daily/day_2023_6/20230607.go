package day_2023_06

// 关于 channel，下面语法正确的是

/*
	A. var ch chan int
	B. ch := make(chan int)
	C. <- ch
	D. ch <-
*/

// ABC
// A、B 都是声明 channel；C 读取 channel；写 channel 是必须带上值，所以 D 错误。

func Day_2023_06_07() {

}
